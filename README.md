Implementation of an ArcGIS Server Object Interceptor using the ArcGIS Enterprise SDK for .NET. 
SOI logs the username, resource (layer ids), and GlobalIds of features returned from the query to a Map (or Feature) Service's REST endpoint.
